import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Ponferrap.app',
  appName: 'Ponferrap',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
